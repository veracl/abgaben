%h1 To-Do-Liste

%h2 Zu erledigen
- if @to_dos.empty?
  Alles erledigt!
- else
  %ul
    - @to_dos.each do |item|
      %li
        = item.name
        %form{:method=>"post", :action=>"/delete"}
          %input{:type=>"hidden", :name=>"todo", :value=>"#{item.name}"}
          %input{:type=>"submit", :value=>"Abhaken"}

%h2 Erledigt

%a{:href=>"/empty"} Leeren


%ul
  - @dones.each do |item|
    %li= item.name

%h2 Add new todo

%form{:method=>"post", :action=>"/new"}
  %input{:type=>"text", :name=>"newtodo"}
  %input{:type=>"submit", :value=>"Submit"}
